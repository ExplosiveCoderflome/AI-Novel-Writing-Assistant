# 当前阶段开发任务：正文生产与问题治理收束

更新时间：2026-08-31
当前分支：`codex/issue-governance-p1-closure`
开发基线：`beta@9bdb5766`（本地 `beta` 尚未推送）
阶段状态：P0 收束完成；P1 正在处理旧任务治理兼容、统一质量债入口与不可达旧逻辑清理

## 阶段目标

把“正文生成 → 审核 → 问题识别 → 修复 → 保存 → 继续运行”收束为一条低耦合、可恢复、可解释的生产链。问题管理只负责给出治理动作，生成与运行模块只执行动作，不在各调用点重复判断、重试或修改任务状态。

两条产品路线不是写死在执行代码中的分支，而是问题管理的预设配置：

1. **连续完成优先**：允许审核与一次修复尝试；局部问题形成质量债并继续，只有明确重规划、无可用正文或运行安全/数据完整性问题才暂停。
2. **质量优先**：无论人工分阶段还是整本自动创作，只要统一治理决定为 `pause_for_manual`，就停在已保存的章节边界等待显式恢复。

共同硬边界：

- 单章自动修复的最大重试次数小于 2，即最多 1 次。
- 局部质量问题不得自动升级为整本书失败或重规划。
- 连续完成优先在整本自动创作已有可用正文时，把局部质量问题降级为质量债并继续。
- 质量优先允许在相同章节边界暂停，但只能通过问题治理快照决定，不得在执行器中另写判断分支。
- 只有结构化治理决定可以要求暂停、失败或重规划。
- 正文保存、章节状态、时间线和任务状态必须在统一生命周期边界内提交。
- 不增加新的执行路线、不引入新的依赖、不恢复“章节合同/分场景多轮正文生成”。

## 已完成任务

### A. 当前分支前置产品任务

- [x] 热门题材雷达支持选择分析榜单与逐本勾选作品。
- [x] 榜单采用固定高度列表，榜单内提供全选/取消全选，页面保留紧凑标题并移除冗余框架。
- [x] 项目低边框视觉规范写入全局开发规则；现有兼容 UI 原语继续保留，但不再新增 shadcn 组件。
- [x] 自动导演预计章节数允许清空后重新输入。
- [x] 自动导演简易模式、专业模式入口移到更明显的位置。
- [x] 专业创作导出支持 TXT。

对应本地提交：`3210a5ff`、`fab0c354`、`ca010796`、`a89c6d0f`、`f2f31e33`、`c5ff1808`、`ec07fa05`、`36be4da4`、`fcd56814`、`04ed970d`、`e0ab3451`。

### B. 问题治理预设与运行边界

- [x] 把“连续完成优先 / 质量优先”实现为问题管理预设，而不是两套硬编码执行流程。
- [x] 两份命名预设覆盖全部问题动作；选择后不再逐项继承全局，只有“恢复继承全局”整体解除本书预设。
- [x] 人工恢复动作遵守当前小说的问题治理策略。
- [x] 移除章节运行前重复的问题评估，避免同一问题被二次判断。
- [x] 章节运行与质量修复共用同一重试预算，自动修复最多 1 次。
- [x] 运行时直接信任已生成的治理动作，不再额外套一层风险判定。
- [x] 缩小自动导演运行前检查职责：只做运行安全与数据完整性检查，不拥有质量重试策略。
- [x] 问题发现改为只读，不在识别阶段顺带修改任务、章节或问题状态。

对应本地提交：`d7d8f281`、`9a929f4a`、`b2faf530`、`0a5185f2`、`8950981f`、`232068e3`、`1724c6df`。

### C. 正文修复、终结与生命周期收束

- [x] 手工修复与自动修复共用正文终结服务，避免修复成功后遗漏状态、摘要或后续同步。
- [x] 正文释放前先完成时间线终结：正常正文写入稳定时间线，可用但有质量债的正文写入降级时间线。
- [x] 时间线检查点写入成功后才释放后续状态，防止正文已完成但时间线仍未提交。
- [x] 删除隐藏的“松散锚点补丁再次调用 LLM”动作；修复路径保持一次补丁尝试，必要时最多一次整章修复。
- [x] 新增章节生命周期服务，统一管理 `Chapter.content`、生成状态与章节状态写入。
- [x] 运行时、修复流和正文终结不再直接分散写章节生命周期字段。
- [x] 补充章节运行边界、章节生产链与生命周期模块说明。

对应本地提交：`cc5d69a6`、`948dd3a5`、`ee979e82`、`1873a958`。

### D. 人工审核与剩余旁路收束

- [x] 人工章节审核不再静默调用规划器；审核结果只返回结构化质量评估与重规划建议。
- [x] 明确的 `stop_for_replan + global_book` 会记录为章节级可恢复检查点，保留正文并等待用户显式重规划。
- [x] 质量标记、修复历史、`generationState` 与 `chapterStatus` 由章节生命周期服务一次提交，质量闭环不再直接写章节表。
- [x] 审计正文主链剩余直接写入与重规划调用：显式用户重规划、结构化局部窗口调整、无可用正文或数据安全失败保留；人工审核自动重规划和质量闭环直接状态写入已移除。
- [x] 版本恢复、用户显式编辑、规划章节创建属于各自受保护的数据边界，不混入章节问题治理执行器。

对应本地提交：本次 P0 1–2 阶段提交。

### E. 热门题材雷达创作基础收束

- [x] AI 市场分析输出结构化题材基底、主要推进模式和可选辅助推进模式。
- [x] 雷达推荐在用户确认后复用统一题材基底库与推进模式库，缺失资产由 AI 结构化候选补充，不再维护雷达专属分类。
- [x] 市场简报持久化统一资源引用，并兼容旧版仅保存信号数组的数据。
- [x] 从雷达进入自动导演时直接展示并补齐推荐创作基础，不覆盖用户手动选择。
- [x] 雷达报告分别提供“加入题材基底库”和“加入推进模式库”，服务端成功后才显示查看入口并定位真实节点。
- [x] 已有资源明确显示“库中已有”而非新增成功；真正新建后主动刷新对应资源库缓存。

### F. 真实章节故障闭环

- [x] 定位真实任务在第 8 章保存后、准备第 9 章路线窗口时失败：路线同步错误要求尚未进入执行期的章节必须已有完整执行合同。
- [x] 路线窗口同步只校验章节大纲；执行合同继续由章节执行前的 JIT 边界补齐和校验。
- [x] 章节异常、滚动规划异常和未知运行异常统一进入一次问题治理；动作失败会向上抛出，动作成功后才记录为已处理。
- [x] 质量问题的继续、暂停或失败动作接入同一控制流，不再只记录检测结果。
- [x] 人工恢复状态同时写回章节批次与书级工作流；后台同步不清除暂停，终态会收束遗留的运行中步骤。
- [x] 非阻断 JIT 预取失败仅记录运行日志，正式执行时重新准备，不再制造重复问题记录。

### G. 2026-08-27 再次出现的真实任务问题

- [x] 已从本地运行库核对任务 `cmtazkoqd0001osv1nh9hrfez`、章节 `cmtazrog400p0osv1a1l3jjk5` 与批次：第 8 章正文仍完整保存（3153 字符，`approved/completed`），不是正文丢失。
- [x] 已确认当前批次 `cmtb92o8300lobcv1modd4z0r` 的实际阻断原因为 `402 Insufficient Balance`；批次状态为 `queued + pendingManualRecovery`，已完成 6/72，等待人工恢复。
- [x] 已确认同一书级任务仍显示 `failed`，并保留旧的 `chapter.draft.write 未满足其完成标准` 摘要；执行详情因此把历史章节合同错误放在当前余额错误之前，造成状态与原因错位。
- [x] **P0：统一任务主状态投影**：章节批次进入 `pendingManualRecovery` 后，`chapter.draft.write` 将其识别为合法暂停，不再用通用完成标准错误覆盖书级恢复状态；书级投影优先展示等待恢复与最新错误。
- [x] **P0：补充余额不足恢复语义**：模型供应端明确返回 HTTP `402` 时直接归类为 `runtime.model_unavailable`，按问题治理预设消费一次重试预算或暂停；恢复继续复用已保存章节事实。
- [x] **P0：增加投影回归**：已覆盖“旧 runtime failed + workflow queued/pendingManualRecovery + 最新余额错误”组合，并验证最新余额错误和恢复入口优先于历史 `chapter.draft.write` 错误。

## 已完成验证

- [x] 共享契约与服务端构建通过，客户端类型检查通过。
- [x] 章节运行、问题治理、自动导演、状态投影与服务边界定向回归通过：155 项中 149 通过、6 项按条件跳过、0 失败；集成保护回归 37 项全部通过。
- [x] 文档清单校验通过：33 份文档、17 个键。
- [x] P0 1–2 定向回归通过：人工审核、质量闭环与服务边界 36 项；生产执行、治理预设、生命周期与恢复 37 项；共享和服务端构建、客户端类型检查通过。
- [x] P0 预设、恢复矩阵与状态投影定向回归通过：服务端主回归 96 项中 95 通过、1 项按条件跳过，正文保存安全边界回归 28 项全部通过，客户端纯状态 26 项全部通过；共享契约和服务端构建通过。
- [x] 恢复验证覆盖服务重启、租约过期、模型暂不可用、正文保存失败、明确重规划和局部质量债；恢复按已保存事实跳过完成章节。
- [x] 任务中心、AI 驾驶舱、简易书架和专业工作台均使用自动导演任务/书级投影；失败、阻塞与待恢复任务无 URL 参数时仍可定位，`workspaceTaskId` 保持为独立人工工作区通道。
- [x] 2026-08-28 人工恢复暂停、后续投影步骤截断、旧失败投影与 `402` 模型不可用分类定向回归 60 项中 54 项通过、6 项按条件跳过、0 失败；共享类型和服务端构建通过。
- [x] 问题治理生命周期快照与后续章节合同恢复改动均已同步至当前分支。
- [x] 未运行浏览器、截图或视觉验收；按项目规则留给用户验收。

## 剩余开发任务

### P0：完成本阶段后才可进入下一轮 beta 集成验收

- [x] **收束人工审核 → 重规划入口**：人工审核不得直接改写为重规划或失败；先生成结构化问题与治理动作，仅当动作明确为 `replan` / `stop_for_replan` 时写入可恢复检查点并暂停。
- [x] **审计剩余旁路**：检查正文生成、审核、修复、保存、恢复入口中是否仍存在绕过问题治理的直接状态写入、直接失败、额外 LLM 重试或直接重规划。
- [x] **补齐两套预设的端到端回归**：
  - 连续完成优先：局部审核失败、一次修复失败、可用正文降级后继续下一章。
  - 质量优先：人工分阶段与整本自动创作中的同类问题都按冻结策略暂停、通知人工，处理后从检查点继续。
  - 两条路线都必须验证自动修复不超过 1 次。
- [x] **补齐恢复矩阵**：Worker 重启、租约过期、模型暂不可用、正文保存失败、明确重规划、已有正文的局部质量债；确认恢复不会重复生成已完成章节。
- [x] **统一状态投影验收**：任务中心、AI 驾驶舱、简易模式、专业模式必须展示同一治理动作、质量债、暂停原因和恢复入口；不得用 `workspaceTaskId` 代替自动导演任务 ID。

### P1：治理兼容与清理

- [x] 评估并补齐旧任务治理快照的兼容读取：真实运行库存在缺少治理快照的旧任务；恢复时只读采用当前本书/全局规则，不回写历史任务或预建迁移。
- [ ] 清理已不可达的问题目录项、旧风险阈值接线和无调用方的兼容代码；删除前先用调用关系和回归测试证明不可达。
- [x] 逐个审计 `reportIssue` 调用点，保证治理动作执行成功后才记录为已处理；章节质量闭环、流水线终结、熔断和 Worker 租约恢复均提供真实动作处理器，动作失败会向运行边界传播。
- [ ] 为降级完成的章节补齐可查询的质量债来源、修复尝试次数和后续人工处理入口。

### P2：集成与真实链路验收

- [x] 问题治理生命周期快照已合并到 `beta`，并完成构建、类型检查和组合回归。
- [ ] P1 完成后再次同步 `beta`，重跑相关定向测试和组合回归。
- [ ] 使用脱敏真实小说连续运行至少 10 章，验证局部质量债不中断整本生产、恢复不重复写章、明确重规划与数据安全问题仍会暂停。
- [ ] 验证 `beta` 稳定后再决定是否进入 `main`；本机禁止推送，发布与桌面打包不属于当前阶段。

## 下一步执行顺序

1. 评估旧任务是否存在缺少治理快照的真实兼容需求。
2. 用调用关系证明不可达后，清理旧问题目录项、风险阈值接线和无调用方兼容代码。
3. 逐个审计治理动作记录时机，并补齐质量债的可查询来源和人工处理入口。
4. 完成 P1 后进入 `beta` 集成验证。

## 当前工作区说明

- 已验证快照中的 `client/src/pages/novels/autoDirector/directorCreateStages.ts` 已由本次远端基线覆盖，不再保留额外工作区差异。
- `server/.backups/` 是未跟踪备份目录；不得修改、删除、提交或纳入工程快照。
- 当前功能分支的后续提交仅保留在本地，禁止从本机推送。

## 明确不做

- 不增加第三套问题治理路线。
- 不把两套预设复制成两套执行器。
- 不增加修复重试次数或隐藏 LLM 调用。
- 不为尚未出现的旧数据问题预建迁移框架。
- 不在本阶段扩展新的 UI、创作能力或第三方依赖。
