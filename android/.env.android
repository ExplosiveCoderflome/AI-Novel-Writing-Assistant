# 安卓端环境变量覆盖
# 复制需求：不配置 QDRANT_URL 即禁用 RAG（离线优先）
# 漫画工坊依赖 sharp（安卓编译困难），默认关闭

AI_NOVEL_RUNTIME=web
AI_NOVEL_DATABASE_MODE=sqlite
AI_NOVEL_DISABLE_COMIC=1
AI_NOVEL_DISABLE_RAG=1

# 如需后台常驻 + LAN 访问（同一 WiFi 下 PC 调试）可取消下一行注释
# ALLOW_LAN=1

# 数据落盘目录（Termux 下可在 .bashrc 预置；nodejs-mobile 下由 App 注入）
# AI_NOVEL_APP_DATA_DIR=/data/data/com.ainovel.app/files/data
