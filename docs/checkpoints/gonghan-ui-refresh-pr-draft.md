# 宫寒前端刷新 PR 草稿

## Pull Request 标题

```text
style: refresh Gonghan personal frontend experience
```

## Pull Request 描述

```markdown
## 变更概述

这个 PR 整理了 Gonghan-Princess 个人 fork 的前端展示和公开说明，让项目更明确地呈现为「宫寒小说自动化工作台 / Gonghan Novel Studio」。

主要改动：
- 统一 README、文档和应用壳层中的个人版品牌表达。
- 将首页整理为「宫寒创作驾驶舱」，集中展示下一步行动、主推进项目、后台任务信号和最近创作队列。
- 优化移动端顶部栏、底部导航、首页状态卡片和帮助页首屏可读性。
- 补充个人版说明、AI 辅助贡献标注和上游归属说明。

## 技术边界

- 未修改后端 API。
- 未修改路由路径、query key、数据库结构或自动导演状态判断。
- 未新增运行时依赖。
- 前端图标继续使用 lucide-react。
- 改动主要集中在展示层、品牌文案和文档。

## 验证

- `corepack pnpm --filter @ai-novel/client build`
- `git diff --check`
- 已用浏览器检查桌面端和移动端首页截图。

## AI 辅助与许可证说明

本 PR 包含 AI 辅助生成内容，主要用于：
- 品牌文案和说明文档草稿。
- 前端 UI 打磨方案。
- 展示层代码修改建议。

这些内容已经由提交者人工筛选、修改和验证。本 PR 未引入新的第三方代码、素材、数据集、模型权重或其他受额外许可证约束的内容。项目继续遵循上游 AGPL-3.0-only 许可证，并保留原项目来源与归属说明。
```

## 给维护者的中文留言

```text
您好，我基于个人 fork 做了一轮前端展示和文档整理，主要目标是让项目在个人版本里更清楚地呈现为“宫寒小说自动化工作台”，同时保留上游项目来源、许可证和功能边界。

这次改动主要是首页、应用壳层、移动端可读性和公开说明文档，没有改后端接口、数据库、自动导演状态判断或模型路由逻辑。

另外按贡献说明，我也在 PR 中标注了本次包含 AI 辅助生成内容，主要用于文案、文档草稿和 UI 打磨建议；没有引入新的第三方素材或受额外许可证约束的代码。
```

## 提交前检查清单

- [ ] 确认本地只提交源码和文档，不提交 `.codex-logs/`、`server/backups/`、数据库文件或构建产物。
- [ ] 运行 `corepack pnpm --filter @ai-novel/client build`。
- [ ] 运行 `git diff --check`。
- [ ] 打开 `http://localhost:5173` 检查首页。
- [ ] 用手机宽度或浏览器开发者工具检查首页和帮助页。
- [ ] 在 PR 描述中保留 AI 辅助生成标注。
